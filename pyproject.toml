[project]
name = "railife"
version = "0.1.0"
description = "Personal life-data normalization pipelines."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
